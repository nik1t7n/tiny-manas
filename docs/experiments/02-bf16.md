# 02 — BF16 mixed precision

Status: **accepted** after full paired training and raw-output review. Date: 2026-09-04.

## Hypothesis and gates

On this actual M5/MPS/PyTorch 2.13 host, autocast may reduce activation/output
storage and improve step throughput while FP32 parameters, AdamW state and
cross-entropy preserve numerical stability. Do not convert the whole model.
Use `torch.autocast("mps", dtype=torch.bfloat16)` only around forward and loss;
backward and optimizer update run outside. No CPU fallback or FP16 substitution.

First run two fresh models with the same initialization seed, original 27M
configuration, same real training windows, dropout 0.2, optimizer/schedule and
accumulation. Only forward precision changes. 100 optimizer steps each, then
32 identical validation batches evaluated in FP32. Keep validation protected
from training; never read test. The 100-step cap is a stability/cost probe, not
an abbreviated substitute for the final matched training experiment.

Forecast: finite gradients/loss, validation loss within 0.1 nats, at least 5%
step-time reduction or 10% sampled activation-memory reduction. Falsifier:
unsupported operations, nonfinite values, adverse loss trend, or no worthwhile
resource gain. If the probe passes, compare full matched runs before promotion;
final validation degradation above 0.05 nats is unacceptable without a separately
approved tradeoff. Inspect raw generations for final accepted runs.

Record every step, synchronized time and live allocator samples after forward,
backward and update. Those samples are lower bounds, not true peak memory.
Warmup is excluded from reported median step time. No blanket 2x memory/speed
claim: optimizer state and parameters remain FP32. Cap MPS allocation at 65%
of its recommended budget on the 16 GB shared host.

Command: `.venv/bin/python scripts/experiment_bf16.py`.
Source: [PyTorch AMP documentation](https://docs.pytorch.org/docs/stable/amp.html).
Inspect actual output/loss/parameter/optimizer dtypes rather than assuming
every operation is autocast on the selected backend.

## Probe result

Run: `runs/optimization-02-bf16-20260904T052804Z`, source base `ab72944`;
runner preserved in `scripts/experiment_bf16.py`. All 100 optimizer steps per
mode completed with finite gradients. Seed 1337, B=8,T=256, accumulation=2;
probe sampler seed 1337, fixed validation sampler seed 9001 (32 batches).

| Measurement | FP32 | BF16 autocast |
|---|---:|---:|
| Median update seconds, excluding first 10 | 0.514812 | 0.352698 |
| Last 10 mean train loss | 7.095695 | 7.095904 |
| Identical-batch validation loss, FP32 evaluation | 7.135342 | 7.135239 |
| Maximum sampled allocated bytes | 2,298,147,072 | 2,250,727,168 |
| Maximum sampled driver bytes | 3,542,925,312 | 3,594,305,536 |

The BF16 probe had 31.5% lower median step latency (1.46x throughput ratio).
Validation difference was -0.000102 nats. Output dtype was BF16, while loss,
parameters and AdamW states were FP32. Memory did **not** halve: sampled live
allocation fell only 2.1%, and driver allocation increased 1.5%. The FP32
optimizer and full-vocabulary loss remain substantial costs.

This passes the speed/stability gate, not the final quality gate. Sequential
mode order and short warmup training limit the inference; the full comparison
must not reuse these probe weights as if they were trained models.

## Full comparison preregistration

Command: `.venv/bin/python scripts/run_bf16_comparison.py`.
Use the actual shared `train` implementation with one explicit config option,
`training.precision`. Default stays FP32 until acceptance. Autocast surrounds
training forward/loss only; evaluation and generation stay FP32 in both arms.

Two fresh 3000-step runs use original 27M settings and seeds, with early stopping
disabled **in both** arms so both receive 12,288,000 training target positions.
Unlike the probe, the shared training sampler uses the original seed+1 protocol.
Best checkpoint selection is still by scheduled validation loss every 100 steps.
After each run: 100 identical validation batches and the existing 20-sample
generation/memorization audit. Test is not used. No checkpoint is promoted yet.

Full-run gate: BF16 validation no worse than FP32 +0.05 nats; no new nonfinite
behavior or obvious raw-output degradation; >=5% useful throughput benefit.
Inspect all raw samples before accepting. Existing training memory fields sample
after updates, so do not equate them with the probe's within-step samples or a
true peak. Both artifacts preserve their measurement definition.

### Active run and continuation

The full pair started at 2026-09-04 05:30:40 UTC. Controller artifact:
`runs/optimization-02-full-20260904T053040Z/comparison.json`.
First training directory: `runs/optimization-02-fp32-20260904T053041Z`.
The controller starts BF16 automatically **after** FP32 training, validation,
and audit finish. Do not launch another copy while this process is alive.
Read `metrics.jsonl` for progress; `comparison.json` for completed results or
an explicit failure. A `running` file alone is not process-liveness evidence.
The original accepted checkpoint and default precision are untouched.

Before acceptance, re-audit both saved generation files with protocol version 2:
the running controller imported the pre-correction matcher. See
[audit correction](00-audit-correction.md). Do not compare one old-protocol arm
against one corrected-protocol arm, and do not regenerate existing samples.

## Completed FP32 control; BF16 still running

The FP32 control completed 3000 updates in 1548.042 seconds (including its
training-loop evaluation overhead), or 7,937.77 target positions/s. Best
scheduled validation was 4.246215 at update 2900. Independent 100-batch validation
was 4.345732722, top-1 31.5483%, top-5 49.8306%. Both validation losses reproduce
the historical accepted FP32 run; elapsed time is a fresh measurement, not an
improvement claim over historical hardware load.

Re-audited saved outputs in `generation-audit-v2.json` inside the control run:
maximum normalized copied span 9 words; mean repeated-trigram ratio 0.0421294.
All 20 generated token sequences exactly match the frozen original audit.
Manually read every continuation, not just aggregate metrics. Zero-based sample
2 loops on Bakai, sample 14 strongly loops on the forty companions, and samples
0/19 repeat the same group labels. Samples 4/8/12/16/17 sustain short battle-like
action chains but drift between named actors and horses. Sample 7 has no repeated
trigrams yet still lacks clear narrative progression: a zero repetition score
does not establish coherence. Occasional malformed words and abrupt shifts remain
visible. This is a qualitative screening, not a native-speaker fluency rating.

BF16 started automatically in `runs/optimization-02-bf16-20260904T055741Z`.
No BF16 quality conclusion or default change is made from the completed control.

## Full result and decision

The controller completed both arms normally. The preceding progress notes are
historical; there is no remaining O02 training process to resume.

| Measurement | FP32 | BF16 training autocast |
|---|---:|---:|
| Updates / target positions | 3000 / 12,288,000 | 3000 / 12,288,000 |
| Training-loop elapsed seconds | 1548.042 | 1229.405 |
| Target positions per second | 7,937.77 | 9,995.08 |
| Best step | 2900 | 2900 |
| Independent validation loss | 4.345732722 | 4.345779147 |
| Independent validation top-1 | 31.5483% | 31.5532% |
| Independent validation top-5 | 49.8306% | 49.8257% |
| Maximum sampled allocated bytes, after updates | 1,015,085,312 | 671,680,256 |
| Maximum sampled driver bytes, after updates | 4,662,607,872 | 4,679,385,088 |
| Maximum normalized copied words, audit v2 | 9 | 9 |
| Mean repeated-trigram ratio | 0.0421294 | 0.0409729 |

Training-loop elapsed time fell 20.58%; target throughput increased 25.92%.
Validation loss changed by +0.0000464 nats, far inside the predeclared +0.05 gate.
The full-run after-update allocation sample fell 33.83%, but driver allocation
rose 0.36%. This is not a whole-process memory reduction of 33.83%, nor a true
peak measure. The within-step probe remains the more relevant evidence for its
own measurement definition and showed only a 2.1% live-allocation reduction.

Re-audited BF16's saved outputs into `generation-audit-v2.json`. Read all 20 raw
continuations. They retain short epic-style action phrases and the same weak
global continuity as FP32. Sample 2 still loops on Bakai; sample 14's repeated
trigrams worsen from 25.71% to 31.11%, and sample 17 rises from 1.33% to 10.42%.
Other samples improve on repetition, so the lower aggregate is **not** a claim
of improved prose. Sample 15 shifts from repeated group phrases into repeated
names. No new class of output collapse is apparent; this is not a native-speaker
quality certification. Only sample 5 is token-identical between the two models.

The trained weights are distinct: relative parameter L2 difference 0.00447688,
maximum parameter difference 0.0299823 (tied output weights counted once).
Best-checkpoint SHA-256 values:

- FP32: `1afd2a8c6aaeeb167e4403c9852581fdcd47dd6119bc93dde3a5b8495206d259`.
- BF16: `31499eb747c98bcada1c48b12205033cded96573269bc15464ccafd9905e2167`.

**Decision:** accept BF16 for the 27M training configuration. Parameters and
optimizer remain FP32; evaluation/generation stay FP32. Generic configuration
defaults remain FP32 for backward compatibility and old experiments. The original
checkpoint is not overwritten or deployed. Later experiments use the accepted
BF16 best checkpoint recorded in `accepted-state.json`.

Limitations: one initialization seed, sequential rather than interleaved full
runs, shared-host load, limited generation sample size, and no protected test
evaluation during selection. The result supports a local speed optimization,
not better general Kyrgyz language quality.

The reproduction controller now reads the verified frozen source archive's
original config rather than the evolving current config. This prevents duplicate
precision fields or later architectural changes from silently altering O02.
